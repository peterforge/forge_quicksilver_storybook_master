import React from 'react';
import LoadingSpinner from './LoadingSpinner';

export default {
  title: 'Loading Spinner',
  component: LoadingSpinner,
};

export const text = () => <LoadingSpinner></LoadingSpinner>;